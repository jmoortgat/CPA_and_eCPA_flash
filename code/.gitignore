# Local-only notes
HPC_instructions.md

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Jupyter checkpoints
.ipynb_checkpoints/

# macOS
.DS_Store

# Virtual environments
.env
.venv
env/
venv/

# Intermediate scan tables: too large to distribute, and superseded.
# (results/scan_v4_table.npz and results/solution_table.npz ARE distributed —
# REPRODUCING_FIGURES.md depends on them — so they are deliberately absent
# from this list.)
results/scan_v3_table.npz
results/scan_v2_table.npz

# Exclude all parquets by default (intermediate/reproducible outputs)
*.parquet

# But include the tables that ship with the repository
!results/CPA_ELV_all.parquet

# Large data and reference directories
CO2/
paper/*.pdf
paper/*.html
paper/*.webarchive
figures/


# Logs
logs/

# EXP raw text files (parquet versions are in results/)
EXP/
